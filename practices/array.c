#include<stdio.h>
void main()
{
    int i;
    int a[]={12,3,44,28,44,14,22,16,19};
    int large=a[0];
    int size=size of (a);
    for(i=1;i<size;i++)
    {
        if(a[i]>large)
        {
            largest=a[i];
        }
    }
   printf("The largest element of the array is %d",large);
}
